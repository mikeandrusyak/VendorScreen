-r src/requirements.txt
# Pinned exactly: ruff's formatter adds rules between minor releases (0.16
# started formatting Python blocks in Markdown), so an unpinned range can turn
# CI red on a version bump we didn't choose. Bump this deliberately.
ruff==0.16.2
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
